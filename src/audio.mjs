export class AudioFeedback {
  constructor(settings={master:.7,effects:.8,ui:.8}){this.ctx=null;this.last={};this.settings=settings;}
  unlock(){const C=globalThis.AudioContext||globalThis.webkitAudioContext;if(!C)return;this.ctx ||= new C();if(this.ctx.state==='suspended')this.ctx.resume();}
  tone(freq,duration=.12,volume=.035,type='sine'){if(!this.ctx)return;const o=this.ctx.createOscillator(),g=this.ctx.createGain(),t=this.ctx.currentTime;o.type=type;o.frequency.setValueAtTime(freq,t);g.gain.setValueAtTime(volume*this.settings.master*this.settings.effects,t);g.gain.exponentialRampToValueAtTime(.0001,t+duration);o.connect(g).connect(this.ctx.destination);o.start(t);o.stop(t+duration);}
  station(){this.tone(620,.12,.035);this.tone(820,.28,.025);}
  update(state,input){if(input.thrust&&!this.last.thrust)this.tone(105,.18,.025,'sawtooth');if(input.brake&&!this.last.brake)this.tone(74,.2,.025,'triangle');if(input.fire&&!this.last.fire)this.tone(180,.08,.025,'square');if(state.gravityEntry&&!this.last.gravity)this.tone(330,.35,.025);if(state.dockMessage==='UTRZYMAJ'&&this.last.dock!=='UTRZYMAJ')this.tone(520,.12,.03);if(state.stationIndex>(this.last.station||0))this.station();if(state.slingshot&&!this.last.slingshot)this.tone(940,.3,.04);if(state.events.at(-1)?.type==='goose'&&state.events.at(-1)?.tick!==this.last.goose)this.tone(210,.3,.04,'square');this.last={thrust:input.thrust,brake:input.brake,fire:input.fire,gravity:state.gravityEntry,dock:state.dockMessage,station:state.stationIndex,slingshot:state.slingshot,goose:state.events.at(-1)?.tick};}
}
